apiVersion: apps/v1
kind: Deployment
metadata:
  name: cloudsurge-deployment
  labels:
    app: cloudsurge
    type: frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: cloudsurge
  template:
    metadata:
      labels:
        app: cloudsurge
    spec:
      containers:
      - name: cloudsurge
        image: prmd77/cloudsurgeimag:312
        ports:
        - containerPort: 80
